let jmeno = prompt('Jake je base jmeno');
let vek = prompt ('Jaky je vas vek');

if (vek < 18) {
console.log
'Vase jmeno je (jmeno), je vam (vek), a tak muzete ridit auto az za (18 - vek) let'
);
} else {
  console.log('Vase jmeno je (jmeno, je vam (vek let a jiz muzete ridit');
}
